#!/bin/sh

set -e

init_script_error_handler()
{
	return 0
}

# Automatically added by dh_installinit/13.3.4
if [ -x "/etc/init.d/isc-dhcp-server" ] && [ "$1" = remove ]; then
	invoke-rc.d isc-dhcp-server stop || true
fi
# End automatically added section


exit 0
